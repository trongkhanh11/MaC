<div class="card">
  {{!-- <h4 class="card-header d-flex justify-content-between">
    Categories
    <a class="btn btn-sm btn-outline-primary" href="/admin/categories" role="button">
      <i class="fa fa-plus" aria-hidden="true"></i>
      Add Category
    </a>
  </h4> --}}
  {{#if this.empty}}
  <div class="card-body">
    <h5 class="card-title">
      Không có dữ liệu!
    </h5>
  </div>
  {{else}}
  <div class="card-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th class="text-center">#</th>
          <th>Category</th>
          <th>&nbsp;</th>
        </tr>
      </thead>
      <tbody>
        {{#each this.POST_CHORD}}
        <tr>
          <td scope="row" class="text-center">{{MAPOST}}</td>
          <td>{{CHORD}}</td>
          {{!-- <td class="text-right">
            <a class="btn btn-sm btn-outline-primary" href="/admin/categories/edit?id={{CatID}}" role="button" title="Edit">
              <i class="fa fa-pencil" aria-hidden="true"></i>
            </a>
          </td> --}}
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{/if}}
</div>